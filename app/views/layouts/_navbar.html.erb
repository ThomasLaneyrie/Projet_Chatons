<!-- Navbar Start -->
<div class="container-fluid">
    <div class="row border-top px-xl-5">
        <div class="col-lg-3 d-none d-lg-block">
            <a class="btn shadow-none d-flex align-items-center justify-content-between bg-primary text-white w-100" data-toggle="collapse" href="#navbar-vertical" style="height: 65px; margin-top: -1px; padding: 0 30px;">
                <h6 class="m-0">Categories</h6>
                <i class="fa fa-angle-down text-dark"></i>
            </a>
            <nav class="collapse position-absolute navbar navbar-vertical navbar-light align-items-start p-0 border border-top-0 border-bottom-0 bg-light" id="navbar-vertical" style="width: calc(100% - 30px); z-index: 1;">
                <div class="navbar-nav w-100 overflow-hidden" style="height: auto">
                    <div class="nav-item dropdown">
                        <a href="#" class="nav-link" data-toggle="dropdown">Photos <i class="fa float-right mt-1"></i></a>
                        <a href="#" class="nav-link" data-toggle="dropdown">Mugs (En cours de création) <i class="fa float-right mt-1"></i></a>
                        <a href="#" class="nav-link" data-toggle="dropdown">Magnets (En cours de création) <i class="fa float-right mt-1"></i></a>
                        <a href="#" class="nav-link" data-toggle="dropdown">Stickers (En cours de création) <i class="fa float-right mt-1"></i></a>
                    </div>
                </div>
            </nav>
        </div>
        <div class="col-lg-9">
            <nav class="navbar navbar-expand-lg bg-light navbar-light py-3 py-lg-0 px-0">
                <a href="" class="text-decoration-none d-block d-lg-none">
                    <h1 class="m-0 display-5 font-weight-semi-bold"><span class="text-primary font-weight-bold border px-3 mr-1">E</span>KittySnap</h1>
                </a>
                <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                    <div class="navbar-nav mr-auto py-0">
                        <%= link_to "Photos", items_path, class: "nav-item nav-link" %>

                        <%= link_to "Contact", contact_path, class: "nav-item nav-link" %>
                    </div>
                    <div class="navbar-nav ml-auto py-0">
                    <% if @current_user == nil %>
                        <%= link_to "Log in", new_user_session_path, class: "nav-item nav-link" %>
                        <%= link_to "Sign up", new_user_registration_path, class: "nav-item nav-link" %>
                    <% else %>
                        <%= link_to "Log out", destroy_user_session_path, class: "nav-item nav-link" %>
                        <div class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle active" data-toggle="dropdown">My account</a>
                        <div class="dropdown-menu rounded-0 m-0">
                             <%= link_to "Profile",user_path(current_user.id), class:"dropdown-item" %>
                             <%= link_to "Shopping Cart", panier_path(@current_cart.id), class:"dropdown-item" %>
                             <%= link_to "Edit Info", edit_user_path(@current_user.id), class:"dropdown-item" %> 
                             <%= link_to "Account Settings", edit_user_registration_path, class:"dropdown-item" %> 
                              
                            <a href="checkout.html" class="dropdown-item">Checkout</a>    
                        </div>
                    <%end%>
                  
                </div>
                    </div>
                </div>
            </nav>
        </div>
    </div>
</div>
<!-- Navbar End -->